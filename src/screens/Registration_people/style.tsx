import { PropsWithChildren } from "react";
import styled from "@emotion/styled";

export const InputContainer = styled('div')(() => {
    return {
        // width: '100%',
        // display: 'grid',
        // gridTemplateColumns: '42px 1fr',
        // marginBottom: 'calc(24px + 4px)',
        // '> input': {
        //     gridColumn: 2
        // }
    }
})